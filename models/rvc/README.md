# RVC models

Place each RVC voice model in its own directory with one `.pth` file and one `.index` file.

```text
models/rvc/my_voice/
├── my_voice.pth
└── my_voice.index
```

The directory name is shown as the selectable model name in the RVC tab. Model and index files are intentionally ignored by Git and must not be committed.
