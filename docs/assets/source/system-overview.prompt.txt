Create a very simple horizontal architecture diagram for a GitHub README, 3:2 aspect ratio, white background, dark navy text, restrained blue and teal accents, very large Japanese labels, only essential elements, thick arrows, no logos, no watermark. Title: 「local-tts-service のしくみ」. Main solid flow with four large cards: 「ブラウザ UI / CLI」 → 「ローカルサービス Node + FastAPI」 → 「TTS モデル Qwen3-TTS / Irodori / GPT-SoVITS」 → 「音声 / 再生 / 履歴」. Above the local service, place one large input card 「参照音声」 with one arrow directly into the local service. Below the TTS model card, show a clearly optional branch using dashed arrows: from TTS model down to 「RVC 声質変換（任意）」 and then back up to the same 「音声 / 再生 / 履歴」 output. The solid direct arrow from TTS model to output must remain, so RVC never looks mandatory. Add one large footer strip with no arrows: 「ローカル保存：モデル・参照音声・生成物」. Important: write 「声質変換」 exactly, never 「音質変換」. No small model subcards, no extra explanations, no storage arrows, no crossed lines. All text must remain readable at 900px display width.
